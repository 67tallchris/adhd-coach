name = "adhd-coach"
compatibility_date = "2026-02-26"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"

[[d1_databases]]
binding = "DB"
database_name = "adhd-coach-db"
database_id = "c266044b-0caa-4c2c-af9a-7499a041e883"
migrations_dir = "drizzle/migrations"

# AI Provider API Keys (set via `wrangler secret put <NAME>`)
# - ANTHROPIC_API_KEY: For Claude AI
# - QWEN_API_KEY: For Qwen AI (DashScope or compatible endpoint)
# - QWEN_BASE_URL: Optional custom base URL for Qwen API (defaults to DashScope)
